
@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1><i class="fa fa-car"></i> รุ่นย่อย</h1>
<hr />

<nav aria-label="breadcrumb">
    <ol class="breadcrumb">
        <li class="breadcrumb-item"><a href="/Home">หน้าแรก</a></li>
        <li class="breadcrumb-item"><a href="/Setting">ตั้งค่า</a></li>
        <li class="breadcrumb-item"><a href="/CarSetting">ข้อมูลเกี่ยวกับรถ</a></li>
        <li class="breadcrumb-item"><a href="/CarSetting/Brand">ยี่ห้อรถ</a></li>
        <li class="breadcrumb-item"><a href="/CarSetting/Generation?brandId=1">รุ่นรถ</a></li>
        <li class="breadcrumb-item"><a href="/CarSetting/Face?brandId=@ViewBag.brandId&generationId=@ViewBag.generationId">โฉมรถ</a></li>
        <li class="breadcrumb-item active" aria-current="page">รุ่นย่อย</li>
    </ol>
</nav>

<div class="card">
    <div class="card-header">
        <h5><i class="fa fa-search"></i> ค้นหาข้อมูล</h5>
    </div>
    <div class="card-body">
        <div class="row">
            <div class="col-md-4">
                <div class="form-group row">
                    <label class="col-sm-4 col-form-label">ชื่อยี่ห้อ</label>
                    <div class="col-sm-8">
                        <input type="text" class="form-control" value="@ViewBag.brandName" readonly />
                    </div>
                </div>
            </div>
            <div class="col-md-4">
                <div class="form-group row">
                    <label class="col-sm-4 col-form-label">ชื่อรุ่นรถ</label>
                    <div class="col-sm-8">
                        <input type="text" class="form-control" value="@ViewBag.generationName" readonly />
                    </div>
                </div>
            </div>
            <div class="col-md-4">
                <div class="form-group row">
                    <label class="col-sm-4 col-form-label">โฉมรถ</label>
                    <div class="col-sm-8">
                        <input type="text" class="form-control" value="@ViewBag.faceName" readonly />
                    </div>
                </div>
            </div>
            <div class="col-md-4">
                <div class="form-group row">
                    <label class="col-sm-4 col-form-label">ชื่อรุ่นย่อยรถ</label>
                    <div class="col-sm-8">
                        <input type="text" class="form-control" value="" id="textSearch" />
                    </div>
                </div>
            </div>
            <div class="col-md-8 text-right">
                <button class="btn btn-primary" onclick="ReloadDatatable();"><i class="fa fa-search"></i> ค้นหา</button>
            </div>
        </div>
    </div>
</div>

<div class="card mt-1">
    <div class="card-header">
        <h5><i class="fa fa-clipboard-list"></i> ข้อมูลรุ่นย่อย</h5>
    </div>
    <div class="card-body">
        <div class="row">
            <div class="col-md-12">
                <button type="button" class="btn btn-success" data-toggle="modal" data-target="#addModal" onclick="Add(0);"><i class="fa fa-plus"></i> เพิ่มรุ่นย่อยรถ</button>
            </div>
        </div>
        <div class="row mt-1">
            <div class="col-md-12">
                <table id="subFaceTable" class="table table-bordered" style="width:100%">
                    <thead>
                        <tr>
                            <th>ลำดับ</th>
                            <th>รุ่นย่อยรถ</th>
                            <th>
                                จัดการ
                            </th>
                        </tr>
                    </thead>
                    @*<tbody>
                        <tr>
                            <td>Hilux Vigo</td>
                            <td class="text-center">
                                <button type="button" class="btn btn-light btn-sm"><i class="fa fa-edit"></i> แก้ไข</button>
                                <button type="button" class="btn btn-danger btn-sm"><i class="fa fa-trash"></i> ลบ</button>
                            </td>
                        </tr>
                    </tbody>*@
                </table>
            </div>
        </div>
    </div>
</div>

<!-- Modal -->
<div class="modal fade" id="addModal" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true" data-backdrop="static">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="exampleModalLabel">เพิ่ม/แก้ไขรุ่นย่อยรถ</h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body">
                <form id="subfaceForm" asp-controller="CarSetting" asp-action="CreateSubface" method="post" data-ajax="true" data-ajax-method="post" data-ajax-complete="completed">
                    <input type="hidden" id="brandId" name="brandId" value="@ViewBag.brandId" />
                    <input type="hidden" id="generationId" name="generationId" value="@ViewBag.generationId" />
                    <input type="hidden" id="faceId" name="faceId" value="@ViewBag.faceId" />
                    <input type="hidden" id="subfaceId" name="subfaceId" />
                    <div class="form-group">
                        <label class="col-sm-12 col-form-label">ชื่อรุ่นย่อยรถ</label>
                        <div class="col-sm-12">
                            <input type="text" class="form-control" id="subfaceName" name="subfaceName" />
                        </div>
                    </div>
                    <div class="row">
                        <div class="col-md-12 text-right">
                            <button type="button" class="btn btn-danger" data-dismiss="modal"><i class="fa fa-times"></i> ปิด</button>
                            <button type="button" class="btn btn-primary" onclick="Save();"><i class="fa fa-save"></i> บันทึก</button>
                        </div>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>

@section scripts{
    <script>
        var datatable;
        $(document).ready(function () {
            Datatable();

            $('#textSearch').keyup(function (e) {
                if (e.keyCode === 13) {
                    ReloadDatatable();
                }
            })
        });
        function Datatable() {
            datatable = $('#subFaceTable').DataTable({
                order: [],
                searching: false,
                ordering: true,
                lengthChange: false,
                pagingType: 'full_numbers',
                ajax: {
                    url: '/CarSetting/GetDatatableSubface',
                    type: 'GET',
                    data: function (d) {
                        d.brandId = $('#brandId').val(),
                        d.generationId = $('#generationId').val(),
                        d.faceId = $('#faceId').val(),
                        d.subfaceName = $('#textSearch').val()
                    },
                    dataSrc: function (data) {
                        return data;
                    },
                    statusCode: {
                        401: function (xhr, error, thrown) {
                            window.location.reload();
                            return false;
                        }
                    }
                },
                columns: [
                    {
                        data: 'subfaceId',
                        className: 'text-center',
                        render: function (data, type, row, meta) {
                            return meta.row + meta.settings._iDisplayStart + 1;
                        }
                    },
                    {
                        data: 'subfaceName',
                    },
                    {
                        data: 'subfaceId',
                        className: 'text-center',
                        render: function (data, type, row) {
                            if (row.carInSubface == 0) {
                                return '<button type="button" class="btn btn-warning btn-sm" data-toggle="modal" data-target="#addModal" onclick="Add(' + row.subfaceId + ')"><i class="fa fa-edit"></i> แก้ไข</button>' +
                                    ' <button type="button" class="btn btn-danger btn-sm" onclick="Delete(' + row.subfaceId + ')"><i class="fa fa-trash"></i> ลบ</button>';
                            } else {
                                return '<button type="button" class="btn btn-warning btn-sm" data-toggle="modal" data-target="#addModal" onclick="Add(' + row.subfaceId + ')"><i class="fa fa-edit"></i> แก้ไข</button>';
                            }
                        }
                    }
                ]
            });
        }
        function ReloadDatatable() {
            datatable.ajax.reload();
        }
        function Add(subfaceId) {
            $.ajax({
                url: '/CarSetting/GetSubfaceById',
                type: 'GET',
                data: {
                    brandId: '@ViewBag.brandId',
                    generationId: '@ViewBag.generationId',
                    faceId: '@ViewBag.faceId',
                    subfaceId: subfaceId
                },
                success: function (data) {
                    $('#subfaceId').val(data.subfaceId);
                    $('#subfaceName').val(data.subfaceName);
                }
            })
        }
        function Delete(subfaceId) {
            $.showConfirm({
                title: "แจ้งเตือน",
                body: "คุณต้องการลบข้อมูล ใช่หรือไม่?",
                textTrue: "ใช่",
                textFalse: "ไม่ใช่",
                onSubmit: function (result) {
                    if (result) {
                        $.ajax({
                            url: '/CarSetting/DeleteSubface',
                            type: 'POST',
                            data: {
                                brandId: '@ViewBag.brandId',
                                generationId: '@ViewBag.generationId',
                                faceId: '@ViewBag.faceId',
                                subfaceId: subfaceId
                            },
                            success: function (data) {
                                ResponseDialog(data.code, window.location.href);
                            }
                        })
                    }
                }
            })
        }
        function Save() {
            $("#subfaceForm").validate({
                errorClass: 'text-danger',
            }); //sets up the validator
            $("#subfaceName").rules("add", {
                required: true,
                messages: {
                    required: 'กรุณากรอกข้อมูล'
                }
            });

            var _checkSubFaceName = true;
            $.ajax({
                url: '/CarSetting/CheckSubfaceName',
                type: 'GET',
                async: false,
                data: {
                    brandId: $('#brandId').val(),
                    generationId: $('#generationId').val(),
                    faceId: $('#faceId').val(),
                    subfaceId: $('#subfaceId').val(),
                    subfaceName: $('#subfaceName').val()
                },
                success: function (data) {
                    _checkSubFaceName = data;
                }
            })
            if (_checkSubFaceName) {
                $('#subfaceForm').submit();
            } else {
                ExistDialog($('#subfaceName').val());
            }

        }
        function completed(res) {
            $('#addModal').modal('hide');
            ResponseDialog(res.responseJSON.code, window.location.href);
        }
    </script>
}
